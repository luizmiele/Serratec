import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
  },
  input: {
    backgroundColor: "#FFF",
    width: "80%",
    padding: 8,
    marginVertical: 8,
  },
});
